plugins {
  id 'com.android.library'
}

apply from: "${rootProject.rootDir}/organicmaps/android/groovy/common-config.gradle"

android {
  namespace 'app.organicmaps.branding'
  // enableKotlin = true  // see app/build.gradle
}
